#ifndef _REQUESTS_H
#define _REQUESTS_H
#include "String.h"
#include "VectorWithMaxSize.h"
using std::cin;
using std::cout;
using std::endl;

void runProgram();
void singleTest();
void executeRequest(Vector<String>& strings, size_t& start, size_t& end);


#endif
